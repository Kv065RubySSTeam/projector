- flash.each do |key, value|
  - if value.include? "You are not authorized"
    .error-flash
      %pre{class: "#{flash_class(key)}"}= value

:javascript
  $(".error-flash").delay(1500).slideUp(1000);