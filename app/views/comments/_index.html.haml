= render @comments
- if @comments.current_page < @comments.total_pages
  = link_to "Load more", board_column_card_comments_path(@column.board, @column, @card.id, page: @comments.current_page + 1), class: "btn btn-primary float-right mt-1 more", remote: true
