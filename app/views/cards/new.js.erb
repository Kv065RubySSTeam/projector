$('.kanban-title-button').on("ajax:success", function() {
  console.log($(this).parent().find('main .kanban-drag'));
  $(this).parent().parent().find('main .kanban-drag').append("<%= j render 'card' %>");
});